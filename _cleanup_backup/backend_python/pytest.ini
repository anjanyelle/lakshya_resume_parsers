[pytest]
addopts = -q --disable-warnings
testpaths = tests
markers =
    integration: tests requiring external services (db, redis, s3)
    performance: load and benchmark tests
    ground_truth: accuracy evaluation tests
