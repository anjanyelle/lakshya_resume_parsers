Software Developer
ABC Tech Solutions Pvt Ltd
Feb 2023 - Present
Built scalable web applications using React.js, Node.js, and REST APIs

Frontend Developer
XYZ Innovations Pvt Ltd
Jan 2021 - Jan 2023
Developed responsive UI components using React.js and Tailwind CSS



# 1. Verify GPU is enabled
import torch
if torch.cuda.is_available():
    print("✅ GPU:", torch.cuda.get_device_name(0))
else:
    print("❌ NO GPU - STOP HERE")
    raise SystemExit("GPU not enabled")

# 2. Clean up
!rm -rf /content/*

# 3. Upload ZIP
from google.colab import files
print("\n📤 Upload Lakshya-Colab-Training.zip...")
uploaded = files.upload()

# 4. Extract
!unzip -q "Lakshya-Colab-Training.zip" -d /content/

# 5. Navigate
%cd /content/ai-service

# 6. Install dependencies
!pip install -q -r requirements.txt
!pip install -q transformers==4.44.0 datasets==2.19.0 accelerate==0.33.0 evaluate==0.4.1 seqeval scikit-learn torch

# 7. Train (will take ~35 minutes on GPU)
print("\n🚀 Starting training on GPU...")
!python training/train.py

# 8. Mount Google Drive
from google.colab import drive
drive.mount('/content/drive')

# 9. Copy model
!mkdir -p "/content/drive/MyDrive/Resume-NER-Models"
!cp -r /content/ai-service/models/resume-ner-deberta "/content/drive/MyDrive/Resume-NER-Models/"

# 10. Verify
!ls -lh "/content/drive/MyDrive/Resume-NER-Models/resume-ner-deberta"

print("\n🎉 Training complete!")